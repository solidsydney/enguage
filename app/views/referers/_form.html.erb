<%= form_for(@referer) do |f| %>
  <% if @referer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@referer.errors.count, "error") %> prohibited this referer from being saved:</h2>

      <ul>
      <% @referer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
